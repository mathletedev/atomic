[package]
name = "atomic-server"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.6.20"
sqlx = { version = "0.7", features = [ "runtime-tokio", "tls-rustls", "postgres" ] }
tokio = { version = "1.0", features = ["full"] }
